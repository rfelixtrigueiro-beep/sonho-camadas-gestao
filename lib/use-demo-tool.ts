'use client';
import {useEffect,useRef} from 'react';
export function useDemoTool(state:unknown){const ref=useRef(state);ref.current=state;useEffect(()=>{const context=(document as Document & {modelContext?:{registerTool:(tool:unknown,options:unknown)=>void|Promise<void>}}).modelContext;if(!context?.registerTool)return;const life=new AbortController();try{void Promise.resolve(context.registerTool({name:'read_demo_order',description:'Consultar pedido fictício, produção e pagamentos da demonstração atual, sem alterar dados.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:true,untrustedContentHint:false},execute(input:unknown){if(!input||typeof input!=='object'||Array.isArray(input)||Object.keys(input).length)throw new Error('Informe um objeto vazio.');return ref.current;}},{signal:life.signal})).catch(()=>{});}catch{}return()=>life.abort()},[])}
